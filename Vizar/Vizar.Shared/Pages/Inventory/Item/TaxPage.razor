@page "/admin/tax"

@using VizarLibrary.Models.Inventory.Item

<PageTitle>Tax Management - Vizar</PageTitle>

@if (_isLoading)
{
    <AnimatedLoader Visible="true" Label="Page is Loading, Please Wait" />
}
else
{
    <div style="display: flex; flex-direction: column; min-height: 100vh;">
        <Header Title="Tax Management" Subtitle="Manage taxes and details" ShowHome="true" ShowBack="true" ShowLogout="true"
                OnHomeClick="NavigateToDashboard" OnBackClick="NavigateBack" OnLogoutClick="Logout">
            <RightContent>
                <IconButton Icon="IconType.Excel" Variant="ButtonVariant.Excel" Title="Export to Excel (Ctrl + E)"
                            Disabled="_isProcessing" OnClick="ExportExcel" />
                <IconButton Icon="IconType.Pdf" Variant="ButtonVariant.Pdf" Title="Export to PDF (Ctrl + P)"
                            Disabled="_isProcessing" OnClick="ExportPdf" />
                <IconButton Icon="IconType.Save" Variant="ButtonVariant.Save" Title="Save Tax (Ctrl + S)"
                            Disabled="_isProcessing" OnClick="SaveTax" />
                <IconButton Icon="IconType.New" Title="New Tax (Ctrl + N)" Disabled="_isProcessing" OnClick="ResetPage" />
            </RightContent>
        </Header>

        <!-- Main Content Container -->
        <div class="page-container">

            <!-- Tax Entry Section -->
            <div class="section-card">
                <div class="section-header">
                    <div class="section-title-wrapper">
                        <svg class="section-icon" xmlns="http://www.w3.org/2000/svg" width="20" height="20"
                             viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"
                             stroke-linejoin="round">
                            <path d="M3 15v4c0 1.1.9 2 2 2h14a2 2 0 0 0 2-2v-4M17 9l-5 5-5-5M12 12.8V2.5" />
                        </svg>
                        <h2 class="section-title">Tax Details</h2>
                    </div>
                </div>
                <div class="section-body">
                    <div class="form-grid">
                        <!-- Tax Name -->
                        <div class="form-field">
                            <label class="field-label">
                                Tax Name <span class="required">*</span>
                            </label>
                            <SfTextBox @bind-Value="_tax.Name" Placeholder="Enter tax name"
                                       FloatLabelType="FloatLabelType.Never" CssClass="custom-textbox" />
                        </div>

                        <!-- Tax Code -->
                        <div class="form-field">
                            <label class="field-label">
                                Tax Code <span class="required">*</span>
                            </label>
                            <SfTextBox @bind-Value="_tax.Code" Placeholder="Enter tax code"
                                       FloatLabelType="FloatLabelType.Never" CssClass="custom-textbox" />
                        </div>

                        <!-- Inclusive and Extra Checkboxes -->
                        <div class="form-field">
                            <label class="field-label">Tax Type (Select one only)</label>
                            <div style="display: flex; gap: 12px;">
                                <div class="checkbox-wrapper">
                                    <SfCheckBox @bind-Checked="_tax.Inclusive" Label="Inclusive" TValue="bool"
                                                CssClass="custom-checkbox" />
                                </div>
                                <div class="checkbox-wrapper">
                                    <SfCheckBox @bind-Checked="_tax.Extra" Label="Extra" TValue="bool"
                                                CssClass="custom-checkbox" />
                                </div>
                            </div>
                        </div>

                        <!-- CGST -->
                        <div class="form-field">
                            <label class="field-label">CGST %</label>
                            <SfNumericTextBox @bind-Value="_tax.CGST" Placeholder="Enter CGST percentage" Format="N2"
                                              FloatLabelType="FloatLabelType.Never" CssClass="custom-textbox" TValue="decimal" Min="0"
                                              Max="100" />
                        </div>

                        <!-- SGST -->
                        <div class="form-field">
                            <label class="field-label">SGST %</label>
                            <SfNumericTextBox @bind-Value="_tax.SGST" Placeholder="Enter SGST percentage" Format="N2"
                                              FloatLabelType="FloatLabelType.Never" CssClass="custom-textbox" TValue="decimal" Min="0"
                                              Max="100" />
                        </div>

                        <!-- IGST -->
                        <div class="form-field">
                            <label class="field-label">IGST %</label>
                            <SfNumericTextBox @bind-Value="_tax.IGST" Placeholder="Enter IGST percentage" Format="N2"
                                              FloatLabelType="FloatLabelType.Never" CssClass="custom-textbox" TValue="decimal" Min="0"
                                              Max="100" />
                        </div>

                        <!-- Remarks -->
                        <div class="form-field">
                            <label class="field-label">Remarks</label>
                            <SfTextBox @bind-Value="_tax.Remarks" Placeholder="Enter remarks (optional)" Multiline="true"
                                       FloatLabelType="FloatLabelType.Never" CssClass="custom-textbox" />
                        </div>
                    </div>
                </div>
            </div>

            <!-- Taxes List Section -->
            <div class="section-card">
                <div class="section-header">
                    <div class="section-title-wrapper">
                        <svg class="section-icon" xmlns="http://www.w3.org/2000/svg" width="20" height="20"
                             viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"
                             stroke-linejoin="round">
                            <path d="M9 11l3 3L22 4" />
                            <path d="M21 12v7a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11" />
                        </svg>
                        <h2 class="section-title">Taxes List</h2>
                    </div>
                    <div class="header-actions">
                        <ToggleButton Disabled="@_isProcessing" IsActive="@_showDeleted"
                                      Variant="ToggleButton.ToggleVariant.Deleted"
                                      OnToggle="ToggleDeleted" />
                    </div>
                </div>
                <div class="section-body grid-body">
                    <SfGrid @ref="_sfGrid" DataSource="@_taxes" AllowPaging="true" AllowSorting="true" AllowFiltering="true"
                            AllowResizing="true" CssClass="professional-grid" Toolbar="@(new List<string>() { "Search" })">
                        <GridPageSettings PageSize="20" PageSizes="@(new string[] { "20", "50", "100", "200", "All" })" />
                        <GridFilterSettings Type="Syncfusion.Blazor.Grids.FilterType.Excel" />
                        <GridColumns>
                            <GridColumn HeaderText="Actions" Width="120" TextAlign="TextAlign.Center"
                                        AllowFiltering="false">
                                <Template>
                                    @{
                                        var tax = (context as TaxModel);

                                        <div class="grid-actions">
                                            @if (tax.Status)
                                            {
                                                <IconButton Icon="IconType.Edit" Variant="ButtonVariant.Edit" Size="ButtonSize.Grid"
                                                            Title="Edit (Insert)" OnClick="@(() => OnEditTax(tax))" />
                                                <IconButton Icon="IconType.Delete" Variant="ButtonVariant.Delete"
                                                            Size="ButtonSize.Grid" Title="Delete (Delete)"
                                                            OnClick="@(() => ShowDeleteConfirmation(tax.Id, tax.Name))" />
                                            }
                                            else
                                            {
                                                <IconButton Icon="IconType.Recover" Variant="ButtonVariant.Recover"
                                                            Size="ButtonSize.Grid" Title="Recover (Delete)"
                                                            OnClick="@(() => ShowRecoverConfirmation(tax.Id, tax.Name))" />
                                            }
                                        </div>
                                    }
                                </Template>
                            </GridColumn>
                            <GridColumn Field="@nameof(TaxModel.Id)" HeaderText="ID" Width="80"
                                        TextAlign="TextAlign.Center" />
                            <GridColumn Field="@nameof(TaxModel.Name)" HeaderText="Name" Width="200" />
                            <GridColumn Field="@nameof(TaxModel.Code)" HeaderText="Code" Width="150" />
                            <GridColumn Field="@nameof(TaxModel.CGST)" HeaderText="CGST %" Width="120" Format="N2"
                                        TextAlign="TextAlign.Right" />
                            <GridColumn Field="@nameof(TaxModel.SGST)" HeaderText="SGST %" Width="120" Format="N2"
                                        TextAlign="TextAlign.Right" />
                            <GridColumn Field="@nameof(TaxModel.IGST)" HeaderText="IGST %" Width="120" Format="N2"
                                        TextAlign="TextAlign.Right" />
                            <GridColumn Field="@nameof(TaxModel.Inclusive)" HeaderText="Inclusive" Width="100"
                                        TextAlign="TextAlign.Center">
                                <Template>
                                    @{
                                        var tax = (context as TaxModel);
                                        <div class="grid-checkbox"
                                             style="@(tax.Inclusive ? "color: #10b981; background: #d1fae5;" : "color: #64748b; background: #f1f5f9;")">
                                            @(tax.Inclusive ? "✓" : "✗")
                                        </div>
                                    }
                                </Template>
                            </GridColumn>
                            <GridColumn Field="@nameof(TaxModel.Extra)" HeaderText="Extra" Width="100"
                                        TextAlign="TextAlign.Center">
                                <Template>
                                    @{
                                        var tax = (context as TaxModel);
                                        <div class="grid-checkbox"
                                             style="@(tax.Extra ? "color: #10b981; background: #d1fae5;" : "color: #64748b; background: #f1f5f9;")">
                                            @(tax.Extra ? "✓" : "✗")
                                        </div>
                                    }
                                </Template>
                            </GridColumn>
                            <GridColumn Field="@nameof(TaxModel.Remarks)" HeaderText="Remarks" Width="300" />
                            <GridColumn Field="@nameof(TaxModel.Status)" HeaderText="Status" Width="100"
                                        TextAlign="TextAlign.Center">
                                <Template>
                                    @{
                                        var tax = (context as TaxModel);
                                        <div class="grid-checkbox"
                                             style="@(tax.Status ? "color: #10b981; background: #d1fae5;" : "color: #dc2626; background: #fee2e2;")">
                                            @(tax.Status ? "✓" : "✗")
                                        </div>
                                    }
                                </Template>
                            </GridColumn>
                        </GridColumns>
                    </SfGrid>
                </div>
            </div>

        </div>

        <Footer />
    </div>
}

<DeleteConfirmationDialog @ref="_deleteConfirmationDialog" EntityName="Tax" IdentifierLabel="Tax Name"
                          IdentifierValue="@_deleteTaxName" IsPermanentDelete="false" Disabled="@_isProcessing" OnConfirm="ConfirmDelete"
                          OnCancel="CancelDelete" />

<RecoverConfirmationDialog @ref="_recoverConfirmationDialog" EntityName="Tax" IdentifierLabel="Tax Name"
                           IdentifierValue="@_recoverTaxName" Disabled="@_isProcessing" OnConfirm="ConfirmRecover" OnCancel="CancelRecover" />

<ToastNotification @ref="_toastNotification" />